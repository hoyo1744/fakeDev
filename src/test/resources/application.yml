
spring:
  jpa:
    hibernate:
      ddl-auto: create

    properties:
      hibernate:
#        stdout으로 로깅
        show_sql: true
        format_sql: true

logging.level:
  org.hibernate.sql: debug



