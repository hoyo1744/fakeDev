

spring:
  jpa:
    hibernate:
      ddl-auto: create

    properties:
      hibernate:
#        stdout으로 로깅
        show_sql: true
        format_sql: true
  messages:
    basename: errors
  profiles:
    active: "test"


logging.level:
  org.hibernate.sql: debug



